# 🔒 Security Report

**Date:** 2026-05-10 | **Stack:** Unknown / Unknown

## Summary

Critical security posture with 3 high-severity credential exposure risks and 7 medium issues. Hardcoded connection strings for Redis and MongoDB in config files expose production credentials in version control. Insecure HTTP webhook endpoint enables man-in-the-middle attacks on callback data.

## Severity Breakdown

| Severity | Total | Auto-Fixed |
|----------|-------|------------|
| 🟠 high | 3 | 3 |
| 🟡 medium | 7 | 4 |

## Findings

### 🟠 High

**1. Hardcoded Redis Connection String** — `config/default.json:17`
```
"uri": "redis://localhost:6379"
```
Redis connection URI is hardcoded. In production, this may contain credentials that would be exposed in version control. ✅ Auto-fixed

**2. Hardcoded MongoDB Connection String** — `config/default.json:20`
```
"uri": "mongodb://localhost:27017?replicaSet=rs0&directConnection=true"
```
MongoDB connection URI is hardcoded. Production credentials would be exposed in version control. ✅ Auto-fixed

**3. Insecure HTTP Webhook Base URL** — `config/default.json:29`
```
"baseUrl": "http://localhost:3000/plugins/webhooks/callback/"
```
Webhook callback URL uses HTTP instead of HTTPS, allowing man-in-the-middle attacks on webhook data. ✅ Auto-fixed

### 🟡 Medium

**1. MongoDB replica set without authentication visible** — `docker-compose.yml`
MongoDB replica set without authentication visible ⚠️ Manual review

**2. Using specific but older MongoDB version 4.4.6** — `docker-compose.yml`
Using specific but older MongoDB version 4.4.6 ⚠️ Manual review

**3. Cannot fully assess - file truncated, but appears to use body-parser which may need size limits configured** — `src/main.ts`
Cannot fully assess - file truncated, but appears to use body-parser which may need size limits configured ⚠️ Manual review

**4. Swagger API Documentation Exposed in Production** — `config/default.json:6`
```
"active": "1"
```
Swagger API documentation is enabled in production environment, exposing API endpoints and their structure to potential attackers. ✅ Auto-fixed

**5. Server Binding to All Network Interfaces** — `config/default.json:12`
```
"hostname": "0.0.0.0"
```
Server is configured to listen on all network interfaces (0.0.0.0), which may expose the service to unintended networks. ✅ Auto-fixed

**6. Hardcoded Temporal Service Address** — `config/default.json:23`
```
"address": "localhost:7233"
```
Temporal service address is hardcoded, preventing secure configuration management across environments. ✅ Auto-fixed

**7. Hardcoded File Storage Path** — `config/default.json:32`
```
"rootPath": "./devenv/storage/plugins"
```
File storage path is hardcoded with a development-specific path, which is inappropriate for production. ✅ Auto-fixed

## Action Plan

*Generated by Agnixa Recon 2.0 Brain — 2026-05-10T14:51:26.938Z*
