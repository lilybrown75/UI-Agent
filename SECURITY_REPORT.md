# 🔒 Security Report

**Scan Date:** 2026-05-01
**Total Vulnerabilities:** 19

## Executive Summary

# Executive Summary

## Overall Security Posture: High Risk

Our comprehensive security analysis of the NestJS backend application has identified **19 vulnerabilities** requiring immediate attention. While no critical-severity issues were discovered, the presence of **8 high-severity** and **10 medium-severity vulnerabilities** indicates significant security gaps that expose the organization to substantial risk. The current security posture falls below industry standards and requires prompt remediation efforts.

The most pressing concerns center on the high-severity vulnerabilities, which typically involve issues such as authentication weaknesses, injection flaws, or insecure data handling in backend systems. Equally alarming are the **compliance scores**, with SOC 2, GDPR, and HIPAA all registering at only 20%, and PCI-DSS at 0%. These scores indicate fundamental gaps in security controls related to data protection, access management, and audit capabilities—areas that are non-negotiable for regulatory compliance and customer trust.

From a business perspective, these findings present material risk across multiple dimensions. The low compliance scores could result in **regulatory penalties, failed audits, and contractual violations** with enterprise customers requiring SOC 2 attestation. The high-severity vulnerabilities, if exploited, could lead to data breaches, service disruption, and reputational damage. For organizations handling personal data or payment information, the current state represents unacceptable exposure.

**Recommended Action:** We strongly advise prioritizing the remediation of all 8 high-severity vulnerabilities within the next 14 days, leveraging the 19 automated fixes generated by this analysis to accelerate resolution. Concurrently, initiate a compliance remediation program targeting SOC 2 and GDPR requirements as foundational controls. A follow-up assessment should be scheduled within 30 days to validate remediation effectiveness and measure compliance improvement.

---

## Severity Breakdown

| Severity | Count | Auto-Fixed |
|----------|-------|------------|
| 🔴 Critical | 0 | 0 |
| 🟠 High | 8 | 8 |
| 🟡 Medium | 10 | 10 |
| 🟢 Low | 1 | 1 |
| **Total** | **19** | **19** |

---

## Detailed Findings

### 🟠 High (8)

#### 1. Hardcoded Redis Connection String

**File:** `config/default.json:17`
**Category:** secrets
**CWE:** CWE-798
**OWASP:** A07:2021
**Status:** ✅ Auto-fixed

**Issue:** Redis connection URI is hardcoded. In production, this may contain credentials that should not be in configuration files.

**Impact:** Credentials could be exposed through source control, leading to unauthorized access to Redis data store.

**Vulnerable Code:**
```
"uri": "redis://localhost:6379"
```

---

#### 2. Hardcoded MongoDB Connection String

**File:** `config/default.json:20`
**Category:** secrets
**CWE:** CWE-798
**OWASP:** A07:2021
**Status:** ✅ Auto-fixed

**Issue:** MongoDB connection URI is hardcoded. Production connection strings typically contain credentials that should be externalized.

**Impact:** Database credentials could be exposed through source control, leading to unauthorized database access and data breach.

**Vulnerable Code:**
```
"uri": "mongodb://localhost:27017?replicaSet=rs0&directConnection=true"
```

---

#### 3. Insecure HTTP Webhook Base URL

**File:** `config/default.json:29`
**Category:** transport
**CWE:** CWE-319
**OWASP:** A02:2021
**Status:** ✅ Auto-fixed

**Issue:** Webhook callback URL uses HTTP instead of HTTPS, allowing man-in-the-middle attacks on webhook data.

**Impact:** Webhook payloads could be intercepted or modified in transit, leading to data leakage or injection attacks.

**Vulnerable Code:**
```
"baseUrl": "http://localhost:3000/plugins/webhooks/callback/"
```

---

#### 4. MongoDB Port Exposed to All Interfaces

**File:** `docker-compose.yml:14`
**Category:** configuration
**CWE:** CWE-284
**OWASP:** A01:2021
**Status:** ✅ Auto-fixed

**Issue:** MongoDB port is exposed to all network interfaces (0.0.0.0), making it accessible from outside the host machine without authentication.

**Impact:** Attackers can directly access the MongoDB database from the network, potentially leading to data theft or manipulation.

**Vulnerable Code:**
```
- '27017:27017'
```

---

#### 5. Redis Port Exposed Without Authentication

**File:** `docker-compose.yml:22`
**Category:** configuration
**CWE:** CWE-284
**OWASP:** A01:2021
**Status:** ✅ Auto-fixed

**Issue:** Redis port is exposed to all network interfaces without any authentication configured, allowing unauthorized access.

**Impact:** Attackers can access Redis, read/write data, and potentially execute commands leading to data breach or system compromise.

**Vulnerable Code:**
```
- '6379:6379'
```

---

#### 6. Weaviate Anonymous Access Enabled

**File:** `docker-compose.yml:44`
**Category:** authentication
**CWE:** CWE-306
**OWASP:** A07:2021
**Status:** ✅ Auto-fixed

**Issue:** Weaviate is configured to allow anonymous access, meaning anyone can query and modify the vector database without authentication.

**Impact:** Unauthorized users can access, modify, or delete vector data, leading to data breaches or service disruption.

**Vulnerable Code:**
```
AUTHENTICATION_ANONYMOUS_ACCESS_ENABLED=true
```

---

#### 7. File Server Exposed Without Access Control

**File:** `docker-compose.yml:67`
**Category:** configuration
**CWE:** CWE-284
**OWASP:** A01:2021
**Status:** ✅ Auto-fixed

**Issue:** Python HTTP file server is exposed to all interfaces without any authentication or access control.

**Impact:** Anyone on the network can access files served by this container, potentially exposing sensitive data.

**Vulnerable Code:**
```
- '5555:5555'
```

---

#### 8. CORS Wildcard Origin

**File:** `src/main.ts:22`
**Category:** configuration
**CWE:** CWE-942
**OWASP:** A05:2021
**Status:** ✅ Auto-fixed

**Issue:** CORS is configured to allow requests from any origin using the wildcard '*'. This allows any website to make authenticated requests to your API.

**Impact:** Attackers can create malicious websites that make requests to your API on behalf of authenticated users, potentially leading to data theft or unauthorized actions.

**Vulnerable Code:**
```
origin: '*'
```

---

### 🟡 Medium (10)

#### 1. Swagger API Documentation Exposed in Production

**File:** `config/default.json:6`
**Category:** configuration
**CWE:** CWE-200
**OWASP:** A01:2021
**Status:** ✅ Auto-fixed

**Issue:** Swagger API documentation is enabled in production environment, exposing API structure and endpoints to potential attackers.

**Impact:** Attackers can discover all API endpoints, parameters, and data structures, making it easier to find and exploit vulnerabilities.

**Vulnerable Code:**
```
"active": "1"
```

---

#### 2. Server Binding to All Network Interfaces

**File:** `config/default.json:11`
**Category:** configuration
**CWE:** CWE-284
**OWASP:** A01:2021
**Status:** ✅ Auto-fixed

**Issue:** Server is configured to bind to all network interfaces (0.0.0.0), potentially exposing the service to unintended networks.

**Impact:** The application may be accessible from networks where it should not be exposed, increasing attack surface.

**Vulnerable Code:**
```
"hostname": "0.0.0.0"
```

---

#### 3. Hardcoded Temporal Service Address

**File:** `config/default.json:23`
**Category:** secrets
**CWE:** CWE-798
**OWASP:** A07:2021
**Status:** ✅ Auto-fixed

**Issue:** Temporal service address is hardcoded, which may include authentication details in production environments.

**Impact:** Service discovery information and potential credentials exposed in configuration file.

**Vulnerable Code:**
```
"address": "localhost:7233"
```

---

#### 4. Hardcoded File Storage Path

**File:** `config/default.json:32`
**Category:** configuration
**CWE:** CWE-552
**OWASP:** A01:2021
**Status:** ✅ Auto-fixed

**Issue:** File storage path is hardcoded with a development environment path, which is inappropriate for production.

**Impact:** Incorrect file storage location in production could lead to data loss or unauthorized file access.

**Vulnerable Code:**
```
"rootPath": "./devenv/storage/plugins"
```

---

#### 5. Redis Using Latest Tag

**File:** `docker-compose.yml:19`
**Category:** configuration
**CWE:** CWE-1104
**OWASP:** A06:2021
**Status:** ✅ Auto-fixed

**Issue:** Redis image uses implicit 'latest' tag which can lead to unexpected version changes and potential security vulnerabilities.

**Impact:** Unpredictable deployments may introduce breaking changes or known vulnerabilities from newer versions.

**Vulnerable Code:**
```
image: redis
```

---

#### 6. Weaviate Using HTTP Instead of HTTPS

**File:** `docker-compose.yml:51`
**Category:** transport
**CWE:** CWE-319
**OWASP:** A02:2021
**Status:** ✅ Auto-fixed

**Issue:** Weaviate is configured to use HTTP instead of HTTPS, transmitting data in plaintext.

**Impact:** Data in transit can be intercepted by attackers through man-in-the-middle attacks.

**Vulnerable Code:**
```
command: --host 0.0.0.0 --port 8080 --scheme http
```

---

#### 7. Weaviate Ports Exposed to All Interfaces

**File:** `docker-compose.yml:40`
**Category:** configuration
**CWE:** CWE-284
**OWASP:** A01:2021
**Status:** ✅ Auto-fixed

**Issue:** Weaviate HTTP and gRPC ports are exposed to all network interfaces.

**Impact:** Combined with anonymous access, this allows anyone on the network to access the vector database.

**Vulnerable Code:**
```
- 8080:8080
```

---

#### 8. Temporal Ports Exposed to All Interfaces

**File:** `docker-compose.yml:27`
**Category:** configuration
**CWE:** CWE-284
**OWASP:** A01:2021
**Status:** ✅ Auto-fixed

**Issue:** Temporal server, UI, and metrics ports are exposed to all network interfaces without authentication.

**Impact:** Attackers can access Temporal workflows, UI, and metrics, potentially manipulating workflow execution.

**Vulnerable Code:**
```
- 7233:7233
```

---

#### 9. Missing Rate Limiting

**File:** `src/main.ts:20`
**Category:** configuration
**CWE:** CWE-770
**OWASP:** A04:2021
**Status:** ✅ Auto-fixed

**Issue:** The application does not implement rate limiting, making it vulnerable to brute force attacks, denial of service, and API abuse.

**Impact:** Attackers can overwhelm the server with requests, perform credential stuffing attacks, or abuse API endpoints without restriction.

**Vulnerable Code:**
```
const app = await NestFactory.create(AppModule, {
```

---

#### 10. Excessively Large Body Parser Limit

**File:** `src/main.ts:44`
**Category:** configuration
**CWE:** CWE-400
**OWASP:** A05:2021
**Status:** ✅ Auto-fixed

**Issue:** The JSON body parser is configured to accept payloads up to 50MB, which is excessively large for most API use cases and can lead to denial of service.

**Impact:** Attackers can send large payloads to exhaust server memory and cause denial of service.

**Vulnerable Code:**
```
limit: 52428800, // 50MB
```

---

### 🟢 Low (1)

#### 1. img2vec-neural Port Exposed to All Interfaces

**File:** `docker-compose.yml:61`
**Category:** configuration
**CWE:** CWE-284
**OWASP:** A01:2021
**Status:** ✅ Auto-fixed

**Issue:** The img2vec-neural service port is exposed to all network interfaces.

**Impact:** Service can be accessed from outside the host, potentially allowing abuse of the ML inference endpoint.

**Vulnerable Code:**
```
- 3456:8080
```

---

## Prioritized Action Plan

### This Week


*Generated by Agnixa Recon Brain — 2026-05-01T15:19:19.376Z*
