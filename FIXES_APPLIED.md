# 🔧 Fixes Applied

**Total:** 16 fixes | **Files:** 2 | **Date:** 2026-05-10

## `config/default.json`

**1. Control Swagger activation via environment variable, defaulting to disabled in production**
Before: `"active": "1"`
After: `Control Swagger activation via environment variable, defaulting to disabled in production`

**2. Use environment variable to configure hostname, allowing restriction to specific interfaces**
Before: `"hostname": "0.0.0.0"`
After: `Use environment variable to configure hostname, allowing restriction to specific interfaces`

**3. Replace with environment variable reference for Redis URI**
Before: `"uri": "redis://localhost:6379"`
After: `Replace with environment variable reference for Redis URI`
Add to .env: `HARDCODED_REDIS_CONNECTION_STRING=your_value_here`

**4. Replace with environment variable reference for MongoDB URI**
Before: `"uri": "mongodb://localhost:27017?replicaSet=rs0&directConnection=true"`
After: `Replace with environment variable reference for MongoDB URI`
Add to .env: `HARDCODED_MONGODB_CONNECTION_STRING=your_value_here`

**5. Replace with environment variable reference for Temporal address**
Before: `"address": "localhost:7233"`
After: `Replace with environment variable reference for Temporal address`
Add to .env: `HARDCODED_TEMPORAL_SERVICE_ADDRESS=your_value_here`

**6. Replace with environment variable that should be configured with HTTPS URL in production**
Before: `"baseUrl": "http://localhost:3000/plugins/webhooks/callback/"`
After: `Replace with environment variable that should be configured with HTTPS URL in production`

**7. Replace with environment variable reference for storage path**
Before: `"rootPath": "./devenv/storage/plugins"`
After: `Replace with environment variable reference for storage path`

## `docker-compose.yml`

**8. Bind to localhost only (127.0.0.1:27017:27017) to restrict access to the host machine.**
Before: `- '27017:27017'`
After: `Bind to localhost only (127.0.0.1:27017:27017) to restrict access to the host machine.`

**9. Bind to localhost only and add password authentication via environment variable.**
Before: `- '6379:6379'`
After: `Bind to localhost only and add password authentication via environment variable.`

**10. Disable anonymous access and configure proper authentication using environment variables.**
Before: `AUTHENTICATION_ANONYMOUS_ACCESS_ENABLED=true`
After: `Disable anonymous access and configure proper authentication using environment variables.`

**11. Configure HTTPS scheme with proper TLS certificates.**
Before: `command: --host 0.0.0.0 --port 8080 --scheme http`
After: `Configure HTTPS scheme with proper TLS certificates.`

**12. Pin to a specific Redis version tag.**
Before: `image: redis`
After: `Pin to a specific Redis version tag.`

**13. Bind to localhost only (127.0.0.1:8080:8080).**
Before: `- 8080:8080`
After: `Bind to localhost only (127.0.0.1:8080:8080).`

**14. Bind to localhost only for all Temporal ports.**
Before: `- 7233:7233`
After: `Bind to localhost only for all Temporal ports.`

**15. Bind to localhost only.**
Before: `- '5555:5555'`
After: `Bind to localhost only.`

**16. Bind to localhost only.**
Before: `- 3456:8080`
After: `Bind to localhost only.`

## Testing Checklist

- [ ] Set required environment variables
- [ ] Run test suite
- [ ] Test affected functionality
- [ ] Deploy to staging first

*Generated by Agnixa Recon 2.0 Brain — 2026-05-10T12:40:54.687Z*
