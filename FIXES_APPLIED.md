# 🔧 Fixes Applied

**Total:** 17 fixes | **Files:** 2 | **Date:** 2026-05-10

## `config/default.json`

**1. Replace with environment variable to control Swagger visibility per environment**
Before: `"active": "1"`
After: `Replace with environment variable to control Swagger visibility per environment`

**2. Replace with environment variable to allow secure configuration per deployment**
Before: `"hostname": "0.0.0.0"`
After: `Replace with environment variable to allow secure configuration per deployment`

**3. Replace with environment variable reference**
Before: `"uri": "redis://localhost:6379"`
After: `Replace with environment variable reference`
Add to .env: `HARDCODED_REDIS_CONNECTION_STRING=your_value_here`

**4. Replace with environment variable reference**
Before: `"uri": "mongodb://localhost:27017?replicaSet=rs0&directConnection=true"`
After: `Replace with environment variable reference`
Add to .env: `HARDCODED_MONGODB_CONNECTION_STRING=your_value_here`

**5. Replace with environment variable reference**
Before: `"address": "localhost:7233"`
After: `Replace with environment variable reference`
Add to .env: `HARDCODED_TEMPORAL_SERVICE_ADDRESS=your_value_here`

**6. Replace with environment variable that should be configured with HTTPS URL in production**
Before: `"baseUrl": "http://localhost:3000/plugins/webhooks/callback/"`
After: `Replace with environment variable that should be configured with HTTPS URL in production`

**7. Replace with environment variable reference**
Before: `"rootPath": "./devenv/storage/plugins"`
After: `Replace with environment variable reference`

## `docker-compose.yml`

**8. Bind to localhost only (127.0.0.1:27017:27017) to restrict access to the host machine.**
Before: `- '27017:27017'`
After: `Bind to localhost only (127.0.0.1:27017:27017) to restrict access to the host machine.`

**9. Bind to localhost only and add Redis password authentication via environment variable.**
Before: `- '6379:6379'`
After: `Bind to localhost only and add Redis password authentication via environment variable.`

**10. Pin Redis to a specific version (e.g., redis:7.2-alpine).**
Before: `image: redis`
After: `Pin Redis to a specific version (e.g., redis:7.2-alpine).`

**11. Disable anonymous access and configure proper authentication using environment variables.**
Before: `- AUTHENTICATION_ANONYMOUS_ACCESS_ENABLED=true`
After: `Disable anonymous access and configure proper authentication using environment variables.`

**12. Configure HTTPS scheme or ensure traffic is encrypted at the network level.**
Before: `command: --host 0.0.0.0 --port 8080 --scheme http`
After: `Configure HTTPS scheme or ensure traffic is encrypted at the network level.`

**13. Bind to localhost only (127.0.0.1:8080:8080).**
Before: `- 8080:8080`
After: `Bind to localhost only (127.0.0.1:8080:8080).`

**14. Bind to localhost only for all Temporal ports.**
Before: `- 7233:7233`
After: `Bind to localhost only for all Temporal ports.`

**15. Bind to localhost only (127.0.0.1:5555:5555).**
Before: `- '5555:5555'`
After: `Bind to localhost only (127.0.0.1:5555:5555).`

**16. Pin to a specific patch version (e.g., python:3.9.18-slim).**
Before: `image: python:3.9-slim`
After: `Pin to a specific patch version (e.g., python:3.9.18-slim).`

**17. Bind to localhost only (127.0.0.1:3456:8080).**
Before: `- 3456:8080`
After: `Bind to localhost only (127.0.0.1:3456:8080).`

## Testing Checklist

- [ ] Set required environment variables
- [ ] Run test suite
- [ ] Test affected functionality
- [ ] Deploy to staging first

*Generated by Agnixa Recon 2.0 Brain — 2026-05-10T15:14:32.552Z*
